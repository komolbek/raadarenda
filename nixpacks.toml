[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd 4event-server && pnpm install"]

[phases.build]
cmds = ["cd 4event-server && pnpm run build"]

[start]
cmd = "cd 4event-server && pnpm run start"
